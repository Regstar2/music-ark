name: musicark_ui
description: "MusicArk Yandex, local library, identity matching, coverage, controlled sync, authorized download, and explicit metadata editor desktop app."
publish_to: 'none'

version: 0.8.2+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_selector: ^1.1.0
  media_kit: ^1.2.6
  media_kit_libs_audio: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
