"""MusicArk v0.4 local library package.

Public imports intentionally stay lazy: storage modules depend on local-library
value objects, so importing the application service here would create a cycle.
"""
